# $cookie rules test 

### Case 1: $cookie rule blocks request cookie

Cookie `case1` should be blocked by rule on request.

### Case 2: $cookie rule blocks response cookie

Cookie `case2` should be blocked by rule on response.

### Case 3: $cookie rule selective blocking request cookie

Cookie `case3` should be blocked by selective rule on request.

### Case 4: $cookie rule selective blocking response cookie

Cookie `case4` should be blocked by selective rule on response.

### Case 5: $cookie allowlist rule bypass request cookie

Cookie `case5` should be set and sent successfully (allowlist rule bypasses blocking).

### Case 6: $cookie allowlist rule bypass response cookie

Cookie `case6` should be set and sent successfully (allowlist rule bypasses blocking).

